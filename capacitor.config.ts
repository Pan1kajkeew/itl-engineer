import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ru.itl.engineer',
  appName: 'ITL Engineer',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
